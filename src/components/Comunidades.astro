---
import { comunidades } from '../data/comunidades';
---

<section id="comunidades" class="section bg-white">
  <div class="container-tight">
    <!-- Header -->
    <div class="max-w-2xl mb-12">
      <p class="text-sm font-medium text-accent uppercase tracking-wide mb-3">
        Cobertura
      </p>
      <h2 class="text-2xl md:text-3xl font-bold text-slate-900 mb-4">
        Subvenciones en toda España
      </h2>
      <p class="text-slate-600">
        Especialistas en las 17 Comunidades Autónomas. Conocemos las convocatorias y requisitos específicos de cada región.
      </p>
    </div>

    <!-- Comunidades Grid -->
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-3">
      {comunidades.map((comunidad) => (
        <a
          href={`/subvenciones/${comunidad.slug}/`}
          class="group border border-slate-200 rounded p-4 hover:border-slate-300 hover:bg-slate-50 transition-colors"
        >
          <p class="text-sm font-medium text-slate-900 group-hover:text-accent">
            {comunidad.nombre}
          </p>
          <p class="text-xs text-slate-500 mt-1">
            Ver ayudas →
          </p>
        </a>
      ))}
    </div>
  </div>
</section>
